Add preliminary window menu (Ctrl+Shift+X)

- manifest: add open-window-menu command; update content script match
  pattern to file:///*menu_extension_menu_page.html?* so query params
  are covered
- background: add openWindowMenu(), getWindows handler (includes current
  window using previousTabId); rename openMenu -> openTabMenu and
  command to open-tab-menu; both menus now use explicit ?type= param
- content: dispatch on ?type= param; set document.title per menu type;
  handle getWindows response reusing makeLi with window active-tab data
- tests: reorganize into tests/tab_menu/ and tests/window_menu/;
  rename openMenuPage -> openTabMenuPage; add openWindowMenuPage;
  add window menu smoke test
